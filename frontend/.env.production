# Ambiente de produção (carregado automaticamente em `vite build` / `npm run build`).
# URL base da API pública (OAuth, links absolutos). Sem barra no final.
VITE_API_ORIGIN=https://ttv-api.evolvetechbr.cloud
